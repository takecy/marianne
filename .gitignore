# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
coverage
*.tsbuildinfo

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Worktrees
.worktrees

# Rust / Tauri target
src-tauri/target
src-tauri/gen/schemas

# Built dmg bundles (output of `pnpm build:dmg`)
dist-bundle

# Tauri signer keys (must never be committed)
.tauri
*.key
*.key.pub

# Local env (Tauri signing env supply via direnv etc.)
.envrc
.env
.env.local
